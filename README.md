# Stream Backend

## Démarrage local (Docker)

```bash
# Démarrer tout
cd stream_backend
docker-compose up -d

# Voir les logs en temps réel
docker logs stream_app -f

# Arrêter tout
docker-compose down
```
